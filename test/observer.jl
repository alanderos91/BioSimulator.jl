# Pre-allocated Observers should have is_preallocated set to true

# Dynamically allocated Observers should have is_preallocated set to false

# Check that all pre-allocated Observers are the same size

# Check that all dynamically allocated Observers are the same size
